[build]
builder = "dockerfile"

[deploy]
startCommand = "litellm --config litellm_config.yaml --port 4000 --host 0.0.0.0"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
